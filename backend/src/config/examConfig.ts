// Re-exports from canonical locations — import directly from types/ or constants/ in new code.
export type { ExamKey, Difficulty, QuestionType, ExamMeta } from '../types';
export { EXAM_CONFIG } from '../constants';
